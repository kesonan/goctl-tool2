parser package fork from go-zero, the original project is [go-zero](https://github.com/zeromicro/go-zero), so make this
repo lighter and easier to use, do not have to import the whole go-zero project.